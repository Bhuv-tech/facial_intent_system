version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Optional: Persist database and model artifacts across container rebuilds/restarts
      - ./data:/app/data
    restart: unless-stopped
